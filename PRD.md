# PRD — 뉴스 검색 사이트

| 항목 | 내용 |
|------|------|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-18 |
| 작성자 | 혜수 (Teamsparta) |
| 상태 | 구현 완료 (v1 스코프) |

---

## 1. 개요

### 1.1 배경

네이버 뉴스 검색 API를 활용해, 키워드로 뉴스 기사를 찾고 목록으로 확인할 수 있는
웹 서비스를 만든다.

네이버 검색 API는 브라우저에서 직접 호출할 수 없다. CORS 정책으로 차단되고, 호출에
필요한 `Client Secret`이 프론트엔드 코드에 노출되기 때문이다. 따라서 **서버가 API를
프록시하는 구조**가 전제 조건이다.

### 1.2 목표

- 키워드 한 번 입력으로 관련 뉴스를 즉시 확인할 수 있다.
- 검색어를 떠올리지 못한 사용자도 인기 키워드 칩으로 바로 탐색을 시작할 수 있다.
- 모든 화면 상태(로딩·결과없음·에러)에서 사용자가 다음에 무엇을 할지 알 수 있다.
- 모바일에서도 데스크톱과 동일한 기능을 쓸 수 있다.
- **API 키가 저장소에 커밋되는 일이 구조적으로 불가능하다.**

### 1.3 성공 기준

| 지표 | 목표 |
|------|------|
| 첫 검색 결과 표시까지 | 1초 이내 (API 응답 기준) |
| 반응형 지원 | 360px ~ 데스크톱 전 구간 가로 스크롤 0 |
| 키 유출 사고 | 0건 (`.gitignore` + pre-commit 훅 이중 차단) |
| 에러 상황에서 사용자 이탈 | 에러 화면에 항상 복구 액션(다시 시도) 제공 |

---

## 2. 사용자 및 시나리오

### 2.1 타깃 사용자

특정 주제의 뉴스를 빠르게 훑어보려는 일반 사용자. 로그인·회원가입은 없다.

### 2.2 핵심 시나리오

**시나리오 A — 명확한 검색어가 있는 경우**

> 사용자가 "반도체" 관련 최신 동향을 확인하려 한다.
> 검색창에 `반도체`를 입력하고 Enter → 결과 목록 확인 → `최신순`으로 정렬 전환 →
> 관심 기사 제목 클릭 → 새 탭에서 원문 확인.

**시나리오 B — 무엇을 검색할지 정하지 않은 경우**

> 사용자가 오늘의 뉴스를 훑어보려 하지만 검색어가 떠오르지 않는다.
> 검색창 아래 인기 키워드 칩에서 `금리`를 클릭 → 즉시 결과 표시 →
> 다른 칩(`환율`)을 눌러 주제를 바꿔가며 탐색.

**시나리오 C — 결과를 더 보려는 경우**

> 1페이지 10건이 부족하다.
> `50개씩`으로 변경하거나 하단 페이지 버튼으로 2, 3페이지 이동.
> 페이지 이동 시 화면 최상단으로 스크롤되어 새 목록을 처음부터 읽는다.

**시나리오 D — 결과를 공유하려는 경우**

> 검색 결과를 동료에게 보내려 한다.
> 주소창 URL을 그대로 복사해서 전달 → 동료가 열면 같은 검색어·정렬·페이지가 복원된다.

---

## 3. 기능 요구사항

### 3.1 검색창 (P0)

| ID | 요구사항 |
|----|----------|
| S-1 | 텍스트 입력 후 `검색` 버튼 클릭 또는 Enter로 검색을 실행한다. |
| S-2 | 첫 진입 시 검색창에 자동 포커스된다. (단, URL에 검색어가 있으면 자동 검색을 우선한다) |
| S-3 | 입력값 앞뒤 공백은 제거한다. |
| S-4 | 빈 값으로 검색하면 요청을 보내지 않고 "검색어를 입력해주세요" 안내를 표시한다. |
| S-5 | 검색 실행 중에는 `검색` 버튼을 비활성화해 중복 요청을 막는다. |
| S-6 | 새 검색은 항상 1페이지부터 시작한다. |

### 3.2 인기 키워드 칩 (P0)

| ID | 요구사항 |
|----|----------|
| C-1 | 검색창 아래에 미리 정의된 키워드 칩을 노출한다. |
| C-2 | 칩 클릭 시 검색창에 해당 키워드가 채워지고 **즉시** 검색이 실행된다. |
| C-3 | 현재 검색어와 일치하는 칩은 활성 상태(테두리·배경 강조)로 표시한다. |
| C-4 | 키워드 목록은 코드 상단 `POPULAR_KEYWORDS` 배열 한 곳만 수정하면 바뀐다. |
| C-5 | 칩 개수가 화면 폭을 넘으면 다음 줄로 자연스럽게 흐른다(wrap). |

**v1 기본 키워드**

`인공지능` `반도체` `금리` `환율` `부동산` `전기차` `스타트업` `프로야구`

> 참고: 실시간 인기 검색어가 아니라 **정적 목록**이다. 네이버 검색 API는 인기 검색어를
> 제공하지 않으며, 실시간화하려면 별도 데이터 소스가 필요하다(범위 외, 8절 참고).

### 3.3 기사 카드 리스트 (P0)

| ID | 요구사항 |
|----|----------|
| L-1 | 검색 결과를 카드 리스트로 세로 배치한다. |
| L-2 | 각 카드는 **제목 / 요약 / 출처 / 작성시각**을 표시한다. |
| L-3 | 제목·요약에서 검색어와 일치하는 부분을 강조(강조색 + 굵게) 표시한다. |
| L-4 | 제목 클릭 시 새 탭(`target="_blank"`, `rel="noopener noreferrer"`)에서 기사를 연다. |
| L-5 | 요약은 최대 3줄(모바일 2줄)까지만 표시하고 넘치면 말줄임한다. |
| L-6 | 출처는 링크의 도메인을 추출해 칩 형태로 표시한다. |
| L-7 | 작성시각은 상대 시간으로 표시한다. (`방금 전` / `N분 전` / `N시간 전` / `N일 전`, 7일 초과 시 `YYYY.MM.DD`) |
| L-8 | 네이버 링크와 언론사 원문 링크가 다를 경우 `원문 보기` 링크를 추가로 제공한다. |

### 3.4 정렬 (P0)

| ID | 요구사항 |
|----|----------|
| O-1 | `정확도` / `최신순` 두 가지를 세그먼트 컨트롤로 제공한다. (API의 `sim` / `date`) |
| O-2 | 기본값은 `정확도`다. |
| O-3 | 정렬 변경 시 현재 검색어를 유지한 채 1페이지부터 다시 조회한다. |
| O-4 | 현재 선택된 정렬은 `aria-pressed`로 표시하고 시각적으로 구분한다. |

### 3.5 페이지네이션 (P0)

| ID | 요구사항 |
|----|----------|
| P-1 | 하단에 페이지 버튼과 이전(`‹`) / 다음(`›`) 버튼을 제공한다. |
| P-2 | 현재 페이지 기준 ±2 페이지와 첫·마지막 페이지를 노출하고, 사이는 `…`으로 축약한다. |
| P-3 | 첫 페이지에서 `‹`, 마지막 페이지에서 `›`는 비활성화한다. |
| P-4 | 페이지당 결과 수를 `10 / 20 / 50`개 중에 선택할 수 있다. |
| P-5 | 페이지 이동 시 화면 최상단으로 스크롤한다. |
| P-6 | **API의 `start` 최대값(1000) 제약을 반영해 이동 가능한 마지막 페이지를 계산한다.** (5.3 참고) |
| P-7 | 결과가 1페이지 이하면 페이지네이션을 표시하지 않는다. |

### 3.6 URL 상태 동기화 (P1)

| ID | 요구사항 |
|----|----------|
| U-1 | 검색어·정렬·페이지·페이지당 개수를 URL 쿼리(`?q=&sort=&page=&display=`)에 반영한다. |
| U-2 | 해당 URL로 진입하면 상태를 복원하고 자동으로 검색을 실행한다. |
| U-3 | `history.replaceState`를 사용해 뒤로가기 히스토리를 오염시키지 않는다. |
| U-4 | 잘못된 파라미터 값은 기본값으로 보정한다. |

---

## 4. 화면 상태 정의

각 상태는 **무엇이 일어났는지**와 **다음에 무엇을 할 수 있는지**를 모두 알려준다.

### 4.1 초기 (검색 전)

- 헤더 + 검색창 + 인기 키워드 칩 노출
- 본문: "어떤 뉴스를 찾으시나요?" / "키워드를 입력하면 관련 뉴스를 보여드립니다."
- 툴바(결과 수·정렬)와 페이지네이션은 숨김

### 4.2 로딩

| 항목 | 정의 |
|------|------|
| 표현 | 카드 형태 **스켈레톤** (제목 1줄 + 본문 3줄), shimmer 애니메이션 |
| 개수 | `min(display, 6)`개 — 실제 결과 레이아웃과 같은 골격 |
| 동시 처리 | `검색` 버튼 비활성화, 페이지네이션 제거, **이전 검색의 결과 수 표시 제거**(잘못된 정보 노출 방지) |
| 경쟁 조건 | 요청마다 `requestId`를 부여하고, 최신 요청이 아닌 응답은 폐기한다 |

### 4.3 결과 없음

- 제목: "검색 결과가 없습니다"
- 본문: "‘{검색어}’ 에 대한 뉴스를 찾지 못했어요. 다른 키워드로 검색해보세요."
- 툴바·페이지네이션 숨김 / 인기 키워드 칩은 계속 노출 → 즉시 다른 탐색 가능

### 4.4 에러

| 상황 | HTTP | 메시지 |
|------|------|--------|
| API 키 미설정 | 500 | "API 키가 설정되지 않았습니다. .env.local 파일에 NAVER_CLIENT_ID / NAVER_CLIENT_SECRET 을 넣어주세요." |
| API 키 오류 | 401→500 | "API 키가 올바르지 않습니다. Client ID / Secret 을 다시 확인해주세요." |
| 일일 한도 초과 | 429 | "일일 호출 한도를 초과했습니다. 잠시 후 다시 시도해주세요." |
| 네이버 API 장애 | 502 | "네이버 API 오류 (HTTP {code}): {상세}" |
| 응답 지연 (10초 초과) | 504 | "네이버 API 응답이 지연되고 있습니다. 다시 시도해주세요." |
| 네트워크 단절 | — | "서버에 연결할 수 없습니다 / 네트워크 상태와 서버 실행 여부를 확인해주세요." |

- 제목은 경고색(빨강)으로 표시
- **모든 에러 화면에 `다시 시도` 버튼을 제공**해 같은 조건으로 재요청할 수 있다
- 서버 로그에는 원본 응답을 남기되, 사용자에게는 키 값 등 민감 정보를 노출하지 않는다

---

## 5. 기술 요구사항

### 5.1 아키텍처

```
브라우저 (public/index.html)
   │  GET /api/news?query=&sort=&page=&display=
   ▼
Express 서버 (server.js)         ← API 키는 여기에만 존재
   │  GET https://openapi.naver.com/v1/search/news.json
   │  헤더: X-Naver-Client-Id / X-Naver-Client-Secret
   ▼
네이버 뉴스 검색 API
```

- 프론트엔드는 빌드 도구 없는 단일 HTML 파일 (HTML + CSS + JS 인라인)
- 서버는 Express, Node 18+ 내장 `fetch` 사용
- **Client Secret은 어떤 경우에도 브라우저로 내려가지 않는다**

### 5.2 내부 API 명세

#### `GET /api/news`

**요청**

| 파라미터 | 타입 | 기본값 | 범위 | 설명 |
|----------|------|--------|------|------|
| `query` | string | (필수) | — | 검색어. 없으면 400 |
| `sort` | string | `sim` | `sim` \| `date` | 그 외 값은 `sim`으로 보정 |
| `page` | number | `1` | 1 ~ `maxPage` | 범위 밖은 clamp |
| `display` | number | `10` | 10 ~ 100 | 범위 밖은 clamp |

**응답 (200)**

```json
{
  "query": "인공지능",
  "sort": "sim",
  "page": 1,
  "display": 10,
  "maxPage": 100,
  "total": 1234,
  "items": [
    {
      "title": "<b>인공지능</b> 관련 기사 제목",
      "description": "기사 요약...",
      "link": "https://n.news.naver.com/...",
      "originallink": "https://...",
      "pubDate": "Mon, 18 Aug 2026 12:00:00 +0900"
    }
  ]
}
```

**에러 (4xx / 5xx)**

```json
{ "error": "사용자에게 그대로 보여줄 수 있는 한국어 메시지" }
```

### 5.3 외부 API 제약 및 대응

| 제약 | 값 | 대응 |
|------|-----|------|
| 일일 호출 한도 | 25,000회 | 429 응답을 전용 메시지로 변환. 공개 배포 시 rate limit 필수(8절) |
| `display` 최대 | 100 | 서버에서 clamp |
| **`start` 최대** | **1000** | 서버가 `maxPage = floor((1000-1)/display)+1`을 계산해 응답에 포함. 프론트는 `min(ceil(total/display), maxPage)`까지만 버튼을 그린다 |
| 응답 인코딩 | 검색어는 `<b>` 태그, 그 외는 HTML 엔티티 | `innerHTML` 금지. `<b>` 기준으로 split → 텍스트 노드로 조립 (5.4) |

> `total`이 100만 건이어도 실제로 열람 가능한 결과는 1,000건까지다. 이 값을 그대로
> 페이지 수로 환산하면 클릭해도 결과가 없는 죽은 페이지 버튼이 생기므로, `maxPage`
> 계산은 필수 요구사항이다.

### 5.4 보안 요구사항

#### SEC-1. API 키는 저장소에 올라갈 수 없다 (P0)

**3중 방어**

1. **환경변수 분리**
   - 실제 키는 `.env.local`에만 둔다.
   - 로드 순서: `.env.local` → `.env` (dotenv는 기존 값을 덮어쓰지 않으므로 `.env.local`이 우선)
   - 저장소에는 **값이 빈 템플릿** `.env.example`만 커밋한다.

2. **`.gitignore` 차단**
   ```gitignore
   .env
   .env.*
   !.env.example
   ```
   - `.env.*` 와일드카드로 `.env.local`, `.env.production` 등 **모든 변형을 사전 차단**한다.
   - 부정 패턴(`!`)으로 템플릿만 예외 허용한다.
   - `*.pem`, `*.key`, `secrets.json`, `credentials.json`도 함께 차단한다.

3. **pre-commit 훅 차단** (`.githooks/pre-commit`, 설치: `git config core.hooksPath .githooks`)
   - `.env` 계열 파일이 스테이징되면 커밋을 **중단**한다. (`git add -f`로 강제 추가한 경우까지 잡는다)
   - 스테이징된 파일에서 `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` 뒤에 8자 이상 값이 붙은 패턴을 찾아 **하드코딩된 키를 차단**한다.
   - 차단 시 해결 방법(`git reset HEAD <file>`)을 함께 안내한다.

> `.gitignore`만으로는 `git add -f`나 코드 내 하드코딩을 막을 수 없다. 훅이 두 번째
> 방어선이며, 템플릿에 값을 넣지 않는 것이 세 번째다.

#### SEC-2. XSS 방지 (P0)

네이버 응답의 제목·요약에는 `<b>` 태그가 포함된다. 이 문자열을 `innerHTML`에 넣으면
API 응답을 경유한 스크립트 주입 경로가 열린다.

- `<b>` / `</b>` 기준으로 문자열을 split
- 홀수 인덱스는 `<strong>` 엘리먼트의 `textContent`로, 나머지는 텍스트 노드로 추가
- HTML 엔티티는 `textarea.value`를 통해 디코딩

**검증 완료**: `&lt;img src=x onerror=alert(1)&gt;` 주입 시 텍스트로만 렌더링되고
`<img>` 엘리먼트는 생성되지 않는다.

#### SEC-3. 기타

- 외부 링크는 `rel="noopener noreferrer"`로 연다.
- 서버 에러 로그에 키 값을 남기지 않는다.
- 배포 환경에서는 `.env.local` 대신 호스팅 플랫폼의 환경변수 설정을 사용한다.

---

## 6. 디자인 요구사항

### 6.1 톤

**모던 다크 모드.** 배경은 거의 검정에 가깝고, 상단에 은은한 그라데이션 글로우를 둔다.
정보 위계는 색의 밝기 차이로 만들고, 강조는 인디고 계열 액센트 한 가지로만 준다.

### 6.2 디자인 토큰

| 역할 | 변수 | 값 |
|------|------|-----|
| 배경 | `--bg` | `#0a0b0f` |
| 표면 | `--surface` | `#161922` |
| 표면(hover) | `--surface-hover` | `#1c202b` |
| 테두리 | `--border` | `#262b38` |
| 테두리(강조) | `--border-strong` | `#333a4a` |
| 본문 텍스트 | `--text` | `#e8eaf0` |
| 보조 텍스트 | `--text-dim` | `#a2a8b8` |
| 흐린 텍스트 | `--text-faint` | `#6b7280` |
| 액센트 | `--accent` | `#6d8fff` |
| 액센트(배경) | `--accent-dim` | `#3d5bbf` |
| 경고/에러 | `--danger` | `#ff7a7a` |
| 라운드 | `--radius` / `--radius-sm` | `14px` / `10px` |

- 폰트: Pretendard (CDN), 실패 시 시스템 한글 폰트로 폴백
- 본문 최대 폭: 780px (한 줄 길이를 읽기 편한 범위로 제한)

### 6.3 반응형

| 구간 | 레이아웃 |
|------|----------|
| ~560px (모바일) | 검색창·버튼 세로 배치(버튼 full width), 인기 키워드 좌측 정렬, 정렬 컨트롤 한 줄 양끝 배치, 카드 제목 16.5px / 요약 2줄 |
| 561px~ (태블릿·데스크톱) | 검색창·버튼 가로 배치, 인기 키워드 중앙 정렬, 카드 제목 17.5px / 요약 3줄 |

**검증 기준**: 360px ~ 1440px 전 구간에서 가로 스크롤이 발생하지 않는다.

### 6.4 접근성

- 정렬·칩 등 토글 컨트롤에 `aria-pressed`, 현재 페이지에 `aria-current="page"` 적용
- 결과 영역에 `aria-live="polite"` — 검색 결과 변경을 스크린리더가 읽는다
- 아이콘 전용 버튼(`‹` `›`)에 `aria-label` 제공
- 입력·선택 요소에 `aria-label` 제공
- 포커스 시 액센트 테두리 + 외곽 링으로 위치를 명확히 표시

---

## 7. 검증 항목

### 7.1 서버

- [x] 정상 검색 요청 → 200 + 정규화된 응답
- [x] `query` 누락 → 400 + 안내 메시지
- [x] `page=9999`, `display=999` → `page=10`, `display=100`으로 clamp
- [x] `page=0`, `display=3` → `page=1`, `display=10`으로 보정
- [x] `sort=date` 정상 전달
- [x] 키 미설정 시 안내 메시지 반환 + 서버 시작 시 경고 로그
- [x] 정의되지 않은 경로 → 404 JSON

### 7.2 프론트엔드

- [x] 인기 키워드 칩 8개 렌더링, 클릭 시 검색 실행 및 활성 상태 1개만 유지
- [x] 검색어 강조(`<strong>`) 정상 표시
- [x] 페이지 이동(1→5), 페이지 버튼 축약 표시(`‹ 1 … 3 4 5 6 7 … 100 ›`)
- [x] 정렬 전환 시 1페이지로 초기화 + URL 반영
- [x] 로딩 스켈레톤 표시 + 검색 버튼 비활성화
- [x] 결과 없음 화면: 툴바·페이지네이션 숨김 확인
- [x] 에러 화면: 메시지 표시 + `다시 시도` 버튼 동작(재요청 성공)
- [x] URL 진입 시 검색어·정렬·페이지 복원
- [x] 콘솔 에러 0건

### 7.3 보안

- [x] `.env.local`이 `.env.*` 규칙으로 무시됨 (`git check-ignore` 확인)
- [x] `.env.example`은 정상 커밋 가능
- [x] `git add -A` 시 `.env` / `.env.local` 미포함
- [x] `git add -f .env.local` 후 커밋 시도 → pre-commit 훅이 차단
- [x] 코드에 키 하드코딩 후 커밋 시도 → pre-commit 훅이 차단
- [x] `.env.local`이 `.env`보다 우선 로드됨
- [x] XSS: 악성 태그 주입 시 텍스트로만 렌더링, `<img>` 0개

### 7.4 반응형

- [x] 390px (모바일) — 가로 스크롤 없음
- [x] 768px (태블릿) — 가로 스크롤 없음
- [x] 1000px+ (데스크톱) — 정상

---

## 8. 범위 외 (v1 제외)

| 항목 | 사유 / 향후 계획 |
|------|------------------|
| 실시간 인기 검색어 | 네이버 검색 API가 제공하지 않음. 자체 검색 로그 집계 또는 외부 데이터 소스 필요 |
| 즐겨찾기(북마크) | v2 후보. 로컬 스토리지 또는 계정 기반 |
| 무한 스크롤 | 페이지네이션으로 대체. `start` 1000 제약 하에서는 이점이 제한적 |
| 검색어 자동완성 | 별도 API 필요 |
| 최근 검색어 기록 | v2 후보 |
| 라이트 모드 | 다크 모드 전용으로 결정. 토큰이 CSS 변수라 추가는 용이 |
| 로그인 / 계정 | 불필요 |
| rate limit | **공개 배포 시 필수.** `express-rate-limit`로 `/api/news` 보호 (미적용 시 제3자가 일일 한도 25,000회를 소진시킬 수 있음) |
| 기사 썸네일 이미지 | 검색 API 응답에 이미지 필드가 없음. 별도 크롤링 필요(정책 검토 대상) |

---

## 9. 파일 구조

```
news-search/
├── server.js              # Express 서버 + 네이버 API 프록시
├── public/
│   └── index.html         # 프론트엔드 (HTML + CSS + JS 단일 파일)
├── .githooks/
│   └── pre-commit         # 키 커밋 차단 훅
├── .env.example           # 값이 빈 템플릿 (커밋 대상)
├── .env.local             # 실제 키 (커밋 안 됨 — .gitignore)
├── .gitignore
├── package.json
├── PRD.md                 # 이 문서
└── README.md              # 실행 가이드
```

---

## 10. 실행 방법

```bash
npm install
cp .env.example .env.local        # 여기에 발급받은 키 입력
git config core.hooksPath .githooks   # 키 커밋 차단 훅 활성화 (권장)
npm start                         # → http://localhost:3000
```

API 키 없이 UI만 확인:

```bash
MOCK=1 npm start
```
